---
description: Designer plans only - chain stops after the plan is returned (no implementation)
---
Use the subagent tool with a chain parameter to execute this workflow for: $@

1. Use the "designer" agent to create the architecture/implementation plan for the feature idea above ($@). Structure the task text in three sections before invoking — Goal / Constraints / Done-criteria — so the designer has an unambiguous brief of what success looks like.

The chain STOPS after the plan is returned: no coder, no reviewer. This is the "review before any code" path — present the designer's full plan to the user for review, and continue with full implementation via prompts/plan-review.md once it is approved. Do not start implementing on your own authority.
